# wetblob# wetblob

Agent-first data lake.

This repo implements **Lake Storage (component 1)**:
- Immutable blobs
- Semantic items
- Graph edges
- Runs
- Task queue

Everything else is out of scope for now.
